# ===========================================
# Travel Helper - Environment Variables
# ===========================================
# 사용법: cp .env.example .env 후 값 수정
#
# 환경별 설정 가이드:
# - LOCAL: Docker로 DB만 실행, API는 로컬에서 개발
# - DEV/PROD: EC2에서 Docker Compose로 전체 실행

# ===========================================
# 기본 설정
# ===========================================
NODE_ENV=development                    # development | production
PORT=3000

# ===========================================
# Database (PostgreSQL)
# ===========================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here    # 반드시 변경
POSTGRES_DB=travel_helper

# Docker 내부 연결 (EC2)
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
DIRECT_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}

# 로컬 개발시 (Docker DB에 직접 연결)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/travel_helper
# DIRECT_URL=postgresql://postgres:postgres@localhost:5432/travel_helper

# ===========================================
# JWT Authentication
# ===========================================
JWT_SECRET=change-this-to-random-string-min-32-chars
JWT_REFRESH_SECRET=change-this-to-another-random-string-32
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# ===========================================
# CORS
# ===========================================
# 로컬: http://localhost:3001
# Vercel: https://your-app.vercel.app
CORS_ORIGIN=http://localhost:3001

# ===========================================
# URLs
# ===========================================
API_URL=http://localhost:3000
ADMIN_URL=http://localhost:3001

# ===========================================
# AI Service
# ===========================================
AI_PROVIDER=groq                        # groq | openai | anthropic
AI_SERVICE_URL=http://localhost:8000    # Docker: http://ai-service:8000
GROQ_API_KEY=gsk_your_groq_api_key

# ===========================================
# Queue (메시지 큐)
# ===========================================
QUEUE_PROVIDER=sync                     # sync (로컬) | sqs (AWS)

# AWS SQS (QUEUE_PROVIDER=sqs 일 때)
AWS_REGION=ap-northeast-2
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SQS_QUEUE_URL=

# ===========================================
# AWS S3 (이미지 저장)
# ===========================================
AWS_S3_BUCKET=

# ===========================================
# External APIs
# ===========================================
EXCHANGE_RATE_API_URL=https://api.exchangerate-api.com/v4/latest

# ===========================================
# Social Login (선택)
# ===========================================
APPLE_CLIENT_ID=
APPLE_TEAM_ID=
APPLE_KEY_ID=
APPLE_PRIVATE_KEY=

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ===========================================
# Monitoring (선택)
# ===========================================
SENTRY_DSN=
